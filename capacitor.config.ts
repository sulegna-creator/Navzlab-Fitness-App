import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.navzlab.fitnessapp',
  appName: 'NAVZLAB Health and Fitness',
  webDir: 'dist'
};

export default config;
